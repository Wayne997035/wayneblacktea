version: "2"
run:
  go: "1.26"
  timeout: 5m

linters:
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell
    - gocritic
    - gosec
    - bodyclose
    - nilnil
    - wrapcheck

linters-settings:
  lll:
    line-length: 140
  gocyclo:
    min-complexity: 15
  gosec:
    excludes: []

issues:
  exclude-rules:
    - path: "_test.go"
      linters:
        - wrapcheck
