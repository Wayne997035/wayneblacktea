VITE_API_BASE_URL=http://localhost:8080
# VITE_API_KEY must match the server's API_KEY env var.
# It is only sent once at startup (POST /api/session) to obtain an httpOnly cookie.
VITE_API_KEY=your-api-key-here
